@model IEnumerable<Livraria.Models.AutoresLivros>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Autor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Livro)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Autor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Livro)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IDAutorLivro">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IDAutorLivro">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IDAutorLivro">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
